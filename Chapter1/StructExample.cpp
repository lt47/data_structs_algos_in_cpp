/** A structure is used for gathering an aggregation of elements. These elements can be different, their grouping is not based on their similarities.
 * R-1.3)
 * It provides references to data only. No methods. It is a data-only, less flexible version of a class. Native to C. 
*/

struct Pair{
    int first;
    double second;
};